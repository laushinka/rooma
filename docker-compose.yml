version: '3'

services:
  db:
    image: mysql:5.7
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=rooma
      - MYSQL_ROOT_HOST=%
  web:
    image: laurie/rooma
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db